enum FolderName {
	DTO = 'dto',
	SRC = 'src',
	TEST = 'test',
	TESTS = 'tests',
	ENUMS = 'enums',
	MODELS = 'models',
	PRISMA = 'prisma',
	INPUTS = 'inputs',
	QUERIES = 'queries',
	EXPECTED_OBJECTS = 'expected-objects',
}

export default FolderName;
