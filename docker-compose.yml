version: '3.8'
services:
  node:
    container_name: node
    build:
      context: ./
      target: '${DOCKER_TARGET}'
    volumes:
      - .:/app
    ports:
      - 3001:3001
      - 5555:5555
    env_file:
      - .env

volumes:
  node:
    name: node
